# React Folder Structure (Minimal)

src/
  components/
  hooks/
  pages/
  services/
  utils/

Rules:
- Components go in `components/`.
- Page-level components go in `pages/`.
- API calls go in `services/`.