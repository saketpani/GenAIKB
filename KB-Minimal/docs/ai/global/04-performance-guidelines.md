# Performance Guidelines (Minimal)

Frontend:
- Avoid unnecessary re-renders.
- Use memoization when appropriate.

Backend:
- Use async/await for I/O operations.
- Avoid heavy operations in controllers.